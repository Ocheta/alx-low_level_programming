0. A file that tests if a function gives the correct output
1. Comment out the part of the code htat causes the output to go into infinite loop
2. program that prints the largest of three integers
3. Program that converts a date to the day of the year
